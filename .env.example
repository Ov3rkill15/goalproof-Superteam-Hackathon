# NOTE: keep this comment as the first line — some editors add a UTF-8 BOM that silently
# corrupts the first variable when parsed (bitten by this once already in a previous project).

# TxLINE (devnet). Mainnet origin: https://txline.txodds.com
TXLINE_ORIGIN=https://txline-dev.txodds.com

# API token from POST /api/token/activate (after on-chain subscribe). Empty = guest JWT only.
TXLINE_API_TOKEN=

# Solana devnet
SOLANA_RPC_URL=https://api.devnet.solana.com
TXLINE_PROGRAM_ID=6pW64gN1s2uqjHkn1unFeEjAwJkPGHoppGvS715wyP2J
KEEPER_WALLET_PATH=./keeper/wallet.json
