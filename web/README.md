# web/ — dashboard

Not scaffolded yet (planned: Next.js). Will show open markets, live scores/odds from the
ingestion feed (live or replay), user positions, and settled results linking to the on-chain
resolution transaction + Merkle proof.

Scaffold when the program takes shape:

```bash
npx create-next-app@latest web --ts --eslint --app --tailwind --no-src-dir
```
